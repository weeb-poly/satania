[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pex = "*"

[packages]
arrow = "*"
discord-py = "*"
html2markdown = "*"
gcsa = "*"
uvloop = "*"

[requires]
python_version = "3.8"

[scripts]
loop = "python -m app.loop"
main = "python -m app.main"
tmux = "tmux new-session -d -s satania 'python -m app.loop'"
docker = "docker-compose -f docker-compose.yml up -d --build --remove-orphans"
pex-build = "./extras/pex.sh"
pex-run-loop = "./satania-loop.pex"
